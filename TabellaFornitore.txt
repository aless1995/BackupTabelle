USE [Fornitori]
-- Create a new table called 'Fornitore' in schema 'dbo'
-- Drop the table if it already exists
IF OBJECT_ID('dbo.Fornitore', 'U') IS NOT NULL
DROP TABLE dbo.Fornitore
GO
-- Create the table in the specified schema
CREATE TABLE dbo.Fornitore
(
   Fornitore     [NVARCHAR](50) NOT NULL   ,
   CodiceFornitore    [NVARCHAR](50)  NOT NULL PRIMARY KEY, -- primary key column,
);
GO